请你从 `FEATURE_LIST*` 中选择未完成的功能点来实现

1. 首先，通过 `git log` 和 `AGENT_LOGGING` 来获取项目的工作进展
2. 选择一个未完成的功能点来实现
3. 不要在 main/master 分支上直接实现功能，而是创建一个新的分支（例如 `feature/[功能点名称]`）
4. 编写完善的测试代码，覆盖所有功能点的边界情况和异常处理
5. 记录你的实现进度和遇到的问题到 `AGENT_LOGGING` 中
6. 完成功能之后，提交所有的变更